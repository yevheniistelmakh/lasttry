
pipeline {
    agent { label 'slave1'
    }
    
    triggers { pollSCM ('* * * * *')
    }
   
    stages {
      stage('variable value') {
        steps {
          sh 'export $IMAGE_VERSION='
	  sh 'echo $ENV1'
          dir('/home/digtal_ocean') {
          sh "pwd"
	  
            }
        } 
 	}
  }
}
